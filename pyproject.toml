[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lms"
version = "0.5.0"
authors = [{ name = "dshilvock", email = "dshilvock@gmail.com" }]
description = "Command line tool for the Lyrion Media Server"
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-2.0-or-later"
license-files = ["LICENSE"]

dependencies = []

[project.optional-dependencies]
dev = [
    "build",
    "coverage",
]

[project.scripts]
lms = "lms:main"

[tool.setuptools]
py-modules = ["lms"]